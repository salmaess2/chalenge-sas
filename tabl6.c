#include <stdio.h>

int main () {
int n,i;
int facteur;
int tab2[i];
printf("donner le nombre des elements");
scanf("%d",&n);
int tab[n];
for( i=0; i<n; i++) {
 printf("saisir les elements");
 scanf("%d",&tab[i]);
 }
for( i=0; i<n; i++) 
tab2[i]=facteur*tab[i];
printf("%d",tab2[i]);
return 0;

}